import React from "react";
import { View, Text } from "react-native";

const ProjectUsersDisplay = (props) => {
	return (
		<View>
			<Text>Users</Text>
		</View>
	);
};

export default ProjectUsersDisplay;
